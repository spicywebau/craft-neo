<input type="hidden" name="{{ name }}" value="">
<div id="{{ id }}" class="neo-input neo-input--{{ neoField.handle }}{{ static ? ' is-static' }}{{ translatable ? ' is-translatable' }}">
    <div class="ni_blocks" data-neo="container.blocks"></div>
    <div data-neo="container.buttons"></div>
</div>
